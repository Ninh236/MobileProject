import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  homeViewStyles: {
    paddingTop: 64,
    paddingBottom: 32,
    paddingHorizontal: 16,
    backgroundColor: '#999',
    minHeight: '100%',
  },

  textStyles: {
    fontSize: 20,
  },
})
