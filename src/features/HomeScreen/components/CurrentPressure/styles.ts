import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  cardStyles: {
    flex: 1,
    minHeight: 180,
  },

  bodyContainer: {
    paddingTop: 32,
  },

  pressureValue: {
    fontSize: 24,
    lineHeight: 28,
    fontWeight: '500',
    color: 'white',
  },

  pressureUnit: {
    fontSize: 16,
    lineHeight: 18,
    fontWeight: '400',
    color: '#989898',
  },
})
