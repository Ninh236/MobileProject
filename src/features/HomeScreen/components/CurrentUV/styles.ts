import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  cardStyles: {
    flex: 1,
    minHeight: 150,
  },

  bodyContainer: {
    paddingTop: 8,
  },

  uvValue: {
    fontSize: 32,
    lineHeight: 36,
    fontWeight: '500',
    color: 'white',
  },

  uvDangerLevel: {
    fontSize: 20,
    lineHeight: 24,
    fontWeight: '500',
    color: 'white',
  },
})
